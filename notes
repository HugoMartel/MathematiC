Idées :
    - Fonction y = f(x) simples
    - Paramétriques
    - 3D ?
    - Fractales
    
    - Style de Sagemath


Technologies :
    - SDL ? SFML ? OpenGL ?
    - CMake
    - Bison/Flex

Afficher plusieurs arrays ? (a vérifier)
les paramètres d'une courbe :
    - couleur
    - xmin, xmax, ymin, ymax
    - pointillés, tirés, continu ?
    - 

